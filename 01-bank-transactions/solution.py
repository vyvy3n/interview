"""
Bank Transaction System.

Implement the `solution` function below.

Read spec/level1.md for the current level's operations.
"""


def solution(queries):
    """
    Process a list of bank queries.

    Args:
        queries: list[list[str]] — each query is [op_name, timestamp, *args]

    Returns:
        list[str] — one response per query (use "" for null/error)
    """
    raise NotImplementedError("Implement me. See spec/level1.md.")
