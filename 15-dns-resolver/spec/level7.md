# Level 7 — Step 7

> **Not directly visible in the screenshots.** The user never scrolled past the start of Step 6 in the assessment description panel, so the full Step 7 prompt was not captured.

The only Step 7 information that was on screen came from the docstring of `resolve_all()` in `dns_exercise.py`:

> "Starting at Step 7, at most `max_workers` calls to `send_query` may be in flight at any time."

That single sentence is everything the screenshots tell us about Step 7. Tests in this directory for Level 7 are inferred from that hint alone, not transcribed from the question.

```
./test.sh 7
```
